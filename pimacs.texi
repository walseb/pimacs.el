\input texinfo   @c -*-texinfo-*-
@c %**start of header
@setfilename pimacs.info
@set VERSION 0.5.0-pre
@settitle Pimacs User Manual v@value{VERSION}
@c %**end of header

@copying
@quotation
Copyright @copyright{} 2026 Anantha Kumaran.

You can redistribute this document and/or modify it under the terms
of the GNU General Public License as published by the Free Software
Foundation, either version 3 of the License, or (at your option) any
later version.

This document is distributed in the hope that it will be useful,
but WITHOUT ANY WARRANTY; without even the implied warranty of
MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE@.  See the GNU
General Public License for more details.

@end quotation
@end copying

@ifhtml
@html
<link rel="alternate" type="text/markdown" href="index.md">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/styles/default.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/highlight.min.js"></script>

<!-- and it's easy to individually load additional languages -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/languages/lisp.min.js"></script>

<link rel="stylesheet" href="https://unpkg.com/highlightjs-copy/dist/highlightjs-copy.min.css">

<script src="https://unpkg.com/highlightjs-copy/dist/highlightjs-copy.min.js"></script>


<script>
hljs.registerAliases(["example"], { languageName: "shell" });
hljs.configure({
  cssSelector: "div.example > pre",
  languageDetectRe: /\b([\w-]+)-preformatted\b/
});

hljs.addPlugin(new CopyButtonPlugin());

hljs.highlightAll();
</script>

<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/@@picocss/pico@@2/css/pico.slate.min.css">
<link rel="stylesheet" type="text/css" href="global.css">
@end html
@end ifhtml

@dircategory Emacs
@direntry
* Pimacs: (pimacs).               An Emacs client for Pi Coding Agent.
@end direntry

@node Top
@top Pimacs User Manual v@value{VERSION}

Pimacs is an Emacs client for @uref{https://pi.dev/, Pi Coding Agent}.  For release history, see the @uref{changelog.html, changelog}.

@menu
* Introduction::
* Setup::
* Doctor::
* Usage::
* UI Customization::
* Sandbox::
* How It Works::
* Keybindings::
* Custom Variables::
* Custom Faces::
@end menu

@node Introduction
@chapter Introduction

Pimacs is an Emacs client for @uref{https://pi.dev/, Pi Coding Agent}.
It provides an interactive interface to the Pi Coding Agent directly
from Emacs.

@node Setup
@chapter Setup

@section Install the Pi Agent

First, install the Pi agent using npm:

@example
npm install -g --ignore-scripts @@earendil-works/pi-coding-agent
@end example

Pimacs does not yet support authentication configuration.  To configure your provider, run @code{pi} in a terminal, then run @code{/login} in Pi.

@section Install @code{pimacs.el}

Pimacs is available from @uref{https://stable.melpa.org/, MELPA Stable}, which
is recommended for installation.  Add it to your package archives, then install
with @code{use-package}:

@lisp
(add-to-list 'package-archives
             '("melpa-stable" . "https://stable.melpa.org/packages/") t)

(use-package pimacs
  :ensure t)
@end lisp

@node Doctor
@chapter Doctor

Run @kbd{M-x pimacs-doctor} to display a dependency report.  It checks that
Pi is available and meets the minimum supported version, and that Tree-sitter
and the Markdown grammars are installed.  When Pi or either grammar is
missing, the report provides a button to install it.

@node Usage
@chapter Usage

Run @kbd{M-x pimacs-chat} from any file in your project to start a Pimacs chat
session.  Use a prefix argument (@kbd{C-u M-x pimacs-chat}) to open a
transient where you can set a session name and root directory.  Setting a root
directory starts the chat there instead of using @code{project.el} to find the
project root.  Run @kbd{M-x pimacs-switch-session} to switch between any
active chat sessions.

The chat buffer is read-only except for the prompt input area.  Use
@kbd{RET} to submit a prompt and @kbd{C-j} to insert a newline.  Press
@kbd{i} from anywhere in the chat buffer to move point to the prompt
input area.

@menu
* Slash Commands::
* File Name Completion::
* Bash::
* Sending Context::
* Images::
* State Lines::
* Managing Sessions::
* Section Visibility::
@end menu

@node Slash Commands
@section Slash Commands

@uref{https://pi.dev/docs/latest/usage#slash-commands, Slash commands}
support completion in the prompt buffer.  Type @kbd{/} and press
@kbd{C-M-i} (or any key bound to completion) to see available commands.

In addition to the commands listed below, extension commands, prompt
templates, and skills registered with the Pi agent can also be invoked
via slash commands.  These are fetched automatically from the agent
and included in the completion list.

Angle brackets @code{<>} denote required arguments, square brackets
@code{[]} denote optional arguments.

@multitable @columnfractions .25 .15 .6
@headitem Command @tab Arguments @tab Description
@item @code{/compact}
@tab @code{[prompt]}
@tab Manually compact context, optionally with custom instructions.
@item @code{/clone}
@tab
@tab Duplicate the current active branch into a new session.
@item @code{/copy}
@tab
@tab Copy last assistant message to clipboard.
@item @code{/cycle-model}
@tab
@tab Cycle through available models.
@item @code{/cycle-thinking-level}
@tab
@tab Cycle through thinking levels.
@item @code{/exit}
@tab
@tab Quit pimacs.
@item @code{/export}
@tab @code{[file]}
@tab Export session to HTML.
@item @code{/fork}
@tab
@tab Create a new session from a previous user message.
@item @code{/model}
@tab
@tab Switch models.
@item @code{/name}
@tab @code{<name>}
@tab Set session display name.
@item @code{/new}
@tab
@tab Start a new session.
@item @code{/quit}
@tab
@tab Quit pimacs.
@item @code{/reload}
@tab
@tab Reload extensions, skills and prompts.
@item @code{/resume}
@tab
@tab Pick from previous sessions.
@item @code{/session}
@tab
@tab Show session file, ID, messages, tokens, and cost.
@item @code{/set-auto-compaction}
@tab
@tab Set auto compaction.
@item @code{/set-auto-retry}
@tab
@tab Set auto retry.
@item @code{/set-follow-up-mode}
@tab
@tab Set follow-up mode.
@item @code{/set-steering-mode}
@tab
@tab Set steering mode.
@item @code{/set-thinking-level}
@tab
@tab Set thinking level.
@end multitable

@node File Name Completion
@section File Name Completion

Type @kbd{@@} followed by a partial file path to trigger file name
completion in the prompt buffer.  The completion backend is controlled
by @code{pimacs-file-completion-backend}.

@node Bash
@section Bash

Prefix a command with @code{!} to run it in Bash.  Use @code{!!} to
run a command without adding it to the conversation context.

@example
! echo "hello"
@end example

@node Sending Context
@section Sending Context

You can send contextual information from any buffer to the Pimacs chat
prompt.

@multitable @columnfractions .35 .65
@headitem Command @tab Description
@item @code{pimacs-send-region} @tab Append the selected region to the prompt input.
@item @code{pimacs-send-filename} @tab Append the current buffer's filename (prefixed with @code{@@}) to the prompt input.
@item @code{pimacs-send-flycheck-errors} @tab Append the Flycheck errors at point to the prompt input.  Falls back to all current errors in the buffer when there are none at point.
@end multitable

@node Images
@section Images

You can attach images to your prompt to provide visual context to the
agent.  Images are displayed as thumbnail previews above the prompt
input area and are sent along with your message.  The agent may also
render images inline in its responses, for example when using the
@code{read} tool on a file that contains images.

@subsection Supported Formats

The following image formats are supported:

@itemize @bullet
@item PNG
@item JPEG
@item GIF
@item WebP
@end itemize


Drag an image file from your file manager and drop it into the Pimacs
chat buffer, or paste a supported image from the clipboard via
@code{yank-media}.  To remove an attached image, click on the
thumbnail with the mouse, or place point on it and press
@kbd{backspace} or @kbd{delete}.

@node State Lines
@section State Lines

The header line displays session context and model information.  The
mode line displays the current agent state.  Customize them with the
@code{pimacs-header-line-format} and @code{pimacs-mode-line-format}
defcustoms.  Both accept strings and state components; use
@code{:spacer} to right-align following components.

@example
(setq pimacs-header-line-format
      '(:context_usage :spacer
        (:model face font-lock-function-name-face)))
@end example

See @code{pimacs-header-line-format} for available components.  A
component may also be a function that receives the current state plist.

Extension status text can be placed in either state line with a
@code{(:status STATUS-KEY ...)} component.  STATUS-KEY is the key used by
the extension's status update.  Text properties after the key customize the
status at that placement.

@example
(setq pimacs-header-line-format
      '(:context_usage " " (:status "xyz-status" face font-lock-warning-face)
        :spacer :model))
@end example

By default, status text also appears beside the prompt.  To display a status
only in a state line, add its key to @code{pimacs-status-widget-hidden-keys}.

@example
(setq pimacs-status-widget-hidden-keys '("xyz-status"))
@end example

@node Managing Sessions
@section Managing Sessions

Use @code{pimacs-switch-session} to switch between active Pimacs chat
sessions using completion.  Use @code{pimacs-list-sessions} to display all
active chat sessions in a sortable tabulated list.  Press @kbd{RET} to visit
the session on the current line and @kbd{g} to refresh the list.  Select a
column header to sort by that column.

Customize the list's columns and initial sort order with
@code{pimacs-list-sessions-table} and @code{pimacs-list-sessions-sort-key}.

@node Section Visibility
@section Section Visibility

Pimacs represents the chat as a hierarchy of sections.  Every non-root section
has a type, a visibility state, and may have a parent or children.  Put point in
a section and run @kbd{M-x pimacs-describe-section} to open a Help buffer with
these details.  The report shows the section's @code{type}, visibility and
user override state, automatic-hiding eligibility, applied face, buffer
positions, and metadata.  Parent and child section types are links; use
@kbd{RET} to follow them.  The @code{type} value is the symbol to use when
configuring section faces or automatic hiding, such as @code{tool-call}.

Use @kbd{TAB} or @kbd{C-i} to toggle the section at point.  Hiding a section
keeps its first line visible, so it can be shown again.  On graphical displays,
the fringe indicator can also be selected with the mouse to toggle visibility.
Use @kbd{S-TAB} to cycle the visibility of the entire chat, or @kbd{1},
@kbd{2}, and @kbd{3} to show progressively more of the tree around point.
@kbd{M-1}, @kbd{M-2}, and @kbd{M-3} do the same for every top-level section.
See @ref{Keybindings} for the complete list of visibility and navigation
commands.

As a conversation grows, Pimacs automatically hides older top-level sections.
@code{pimacs-section-autohide-count} sets how many eligible recent sections
remain visible; its default is 2, and @code{nil} disables automatic hiding.
Use @code{pimacs-section-autohide-filter} to select the eligible section
types.  For example, the following keeps four eligible sections visible while
never automatically hiding user sections:

@lisp
(setq pimacs-section-autohide-count 4
      pimacs-section-autohide-filter '(:exclude user))
@end lisp

Automatic hiding does not override a section whose visibility you explicitly
toggled with @kbd{TAB} or @kbd{C-i}.
Set @code{pimacs-section-visibility-indicators} to @code{nil} to disable the
fringe indicators.

@node UI Customization
@chapter UI Customization

Pimacs renders the chat as a sequence of sections.  Each section type has a
face, so the chat can be styled without changing the Markdown renderer.
Customize @code{pimacs-section-type-faces} to select the face for a section
type, and customize the selected face with @kbd{M-x customize-face}.

The built-in section faces are listed in @ref{Custom Faces}.

@section Common Styling Effects

A section face supplies the base appearance for one kind of chat content.
Only the attributes that you set take effect; leave an attribute unspecified
when the Markdown or role styling should continue to provide it.

For a coloured assistant card that preserves Markdown colours, set only a
background and @code{:extend t}:

@lisp
(set-face-attribute 'pimacs-section-assistant-face nil
                    :background "#18212b"
                    :extend t)
@end lisp

Set @code{:foreground} when every part of a section should use one colour.
Otherwise, leave it as @code{unspecified} to retain Markdown heading, link,
and syntax colours.  Likewise, set @code{:height} or @code{:slant} to make a
section compact or italic, and leave @code{:weight} unspecified when Markdown
bold text should remain bold.

Role labels such as @code{assistant>} and tool names retain their own
recognizable colours by default.  Configure their faces separately when you
want to change them.

@section Cards and Padding

Section faces are also applied to @code{pimacs-section-padding}.  Specify
both @code{:background} and @code{:extend t} for a full-width card background;
the background then also covers empty padding lines.

Thinking defaults to @code{shadow}.  A compact, more subdued thinking card
can be configured as follows:

@lisp
(set-face-attribute 'pimacs-section-thinking-face nil
                    :foreground "#64707c"
                    :background "#131a21"
                    :height 0.9
                    :extend t)
@end lisp

@section Choosing Section Faces

The type-to-face mapping is evaluated when a section is created.  Change one
entry without replacing the other defaults with @code{alist-get}:

@lisp
(setf (alist-get 'tool-result pimacs-section-type-faces)
      'my-pimacs-tool-result-face)
@end lisp

Define a named face when several attributes belong together:

@lisp
(defface my-pimacs-tool-result-face
  '((t :inherit fixed-pitch
       :foreground "#c2cbd5"
       :background "#1a2027"
       :extend t))
  "My Pimacs tool result face.")
@end lisp

Changing a face updates already-displayed text immediately.  Changing
@code{pimacs-section-type-faces} affects newly created sections; recreate the
chat contents to apply a new mapping to existing sections.

@section Prompt Face

The editable portion of the prompt uses @code{pimacs-prompt-face} and can be
customized independently:

@lisp
(set-face-attribute 'pimacs-prompt-face nil
                    :foreground "#d7e0e8"
                    :background "#202936"
                    :extend t)
@end lisp

The @code{user>} prompt label uses @code{pimacs-chat-user-role-face}.

@node Sandbox
@chapter Sandbox

To run Pi inside a sandbox, customize @code{pimacs-executable} and
@code{pimacs-flags}:

@lisp
(setq pimacs-executable "nono")
(setq pimacs-flags '("run" "--silent" "--profile" "pi" "--allow-cwd" "--" "pi"
                 "--tools" "read,bash,edit,write,grep,find,ls"))
@end lisp

@node How It Works
@chapter How It Works

Emacs starts Pi in @uref{https://pi.dev/docs/latest/rpc, RPC mode}.
In this setup, Pi handles the agent logic while Emacs provides the
user interface. Some features, such as @code{/login} and
@code{/logout}, are not
@uref{https://github.com/earendil-works/pi/issues/885, supported}
because they are not currently exposed through the RPC API.

@section Extension UI

Pimacs supports the RPC-compatible extension UI methods: notifications,
selection, confirmation, text input, editing text in an Emacs buffer, setting
the prompt text and chat title, prompt widgets, and status text.  Extension
commands, tools, hooks, and custom messages are also supported.

Pimacs does not support terminal-specific extension UI features, including
@code{ctx.ui.custom()} components, component factories, extension keyboard
shortcuts, or custom TUI rendering callbacks.  Extensions should use the
supported UI methods when running in RPC mode.  See the
@uref{https://pi.dev/docs/latest/extensions, Pi extension documentation} for
the full extension API.

@node Keybindings
@chapter Keybindings

@section Chat Buffer Keybindings

@multitable @columnfractions .2 .3 .5
@headitem Key @tab Command @tab Description
@item @kbd{RET}
@tab @code{pimacs-visit-item}
@tab Jump to the source location at point; with a prefix argument, open
in another window
@item @kbd{C-g}
@tab @code{pimacs-abort}
@tab Abort the current operation
@item @kbd{TAB}, @kbd{C-i}
@tab @code{pimacs-toggle-section}
@tab Toggle visibility of the section at point
@item @kbd{S-TAB}
@tab @code{pimacs-section-cycle-global}
@tab Cycle global section visibility, hiding all sections and then expanding one more level at a time
@item @kbd{1}
@tab @code{pimacs-section-show-level-1}
@tab Show the current section tree at the first level
@item @kbd{2}
@tab @code{pimacs-section-show-level-2}
@tab Show the current section tree through the second level
@item @kbd{3}
@tab @code{pimacs-section-show-level-3}
@tab Show the current section tree through the third level
@item @kbd{M-1}
@tab @code{pimacs-section-show-level-1-all}
@tab Show only headings of all root sections
@item @kbd{M-2}
@tab @code{pimacs-section-show-level-2-all}
@tab Show root sections and only headings of their children
@item @kbd{M-3}
@tab @code{pimacs-section-show-level-3-all}
@tab Show sections through the second level and headings at the third
@item @kbd{n}, @kbd{M-n}
@tab @code{pimacs-goto-next-section}
@tab Move to the next section
@item @kbd{N}
@tab @code{pimacs-goto-next-user-message}
@tab Move to the next user message
@item @kbd{p}, @kbd{M-p}
@tab @code{pimacs-goto-previous-section}
@tab Move to the previous section
@item @kbd{P}
@tab @code{pimacs-goto-previous-user-message}
@tab Move to the previous user message
@item @kbd{l}, @kbd{M-g l}
@tab @code{pimacs-goto-last-section}
@tab Jump to the most recent section
@item @kbd{i}
@tab @code{pimacs-focus-prompt}
@tab Focus the prompt input field
@item @kbd{q}
@tab @code{pimacs-quit-chat}
@tab Quit the chat buffer
@end multitable

@section Prompt Input Keybindings

@multitable @columnfractions .2 .3 .5
@headitem Key @tab Command @tab Description
@item @kbd{C-g}
@tab @code{pimacs-abort}
@tab Abort the current operation
@item @kbd{M-p}
@tab @code{pimacs-previous-prompt}
@tab Recall the previous prompt from history
@item @kbd{M-n}
@tab @code{pimacs-next-prompt}
@tab Recall the next prompt from history
@item @kbd{C-r}
@tab @code{pimacs-search-prompt}
@tab Search prompt history
@item @kbd{RET}
@tab @code{widget-field-activate}
@tab Send the current prompt
@item @kbd{M-RET}
@tab @code{pimacs-send-prompt-alternate}
@tab Send the prompt using the alternate streaming behavior
@item @kbd{M-g l}
@tab @code{pimacs-goto-last-section}
@tab Jump to the most recent section
@item @kbd{S-TAB}
@tab @code{pimacs-section-cycle-global}
@tab Cycle global section visibility
@end multitable

@node Custom Variables
@chapter Custom Variables

@c custom-variables-start

@defopt pimacs-status-widget-hidden-keys @code{nil}

Status keys to hide from the default status widget.

Hidden statuses remain available to @code{(:status STATUS-KEY ...)} components
in @code{pimacs-header-line-format} and @code{pimacs-mode-line-format}.  Hover over a
status in the default widget to see its key.
@end defopt

@defopt pimacs-file-completion-backend @code{'project}

Completion backend for @-prefixed file paths in prompts.
@code{project} uses @code{project-files} to list files in the current project.
@code{file} uses @code{file-name-all-completions} to list files under the project root.
@end defopt

@defopt pimacs-prompt-history-max-size @code{500}

Maximum number of prompt history entries to keep.
@end defopt

@defopt pimacs-resume-max-sessions @code{100}

Maximum number of recent sessions to list when resuming a session.
@end defopt

@defopt pimacs-prompt-streaming-behavior @code{'followUp}

Default streaming behavior for prompts.

@code{steer}: Queue the message while the agent is running.  It is delivered
after the current assistant turn finishes executing its tool calls,
before the next LLM call.

@code{followUp}: Wait until the agent finishes.  Message is delivered only
when agent stops.
@end defopt

@defopt pimacs-markdown-renderer @code{#'pimacs--render-markdown}

Function used to render Markdown content.

Pi calls it as @code{(RENDERER :create)}, @code{(RENDERER :stream STATE TEXT)},
@code{(RENDERER :final STATE TEXT)}, and @code{(RENDERER :destroy STATE)}.  @code{:create}
returns opaque renderer state.  @code{:stream} and @code{:final} return @code{:append},
@code{:delete}, and @code{:replace-suffix} operations.  @code{:destroy} releases resources.

The default uses the incremental Tree-sitter Markdown renderer.  It requires
the Markdown and Markdown Inline Tree-sitter grammars.
@end defopt

@defopt pimacs-thinking-renderer @code{#'pimacs--render-thinking-default}

Function used to render assistant thinking content.

It implements the same @code{:create}, @code{:stream}, @code{:final}, and @code{:destroy}
renderer protocol as @code{pimacs-markdown-renderer}.

The default preserves the legacy, dimmed plain-text rendering.  Set this to
@code{pimacs--render-thinking-markdown} to render thinking as Markdown.
@end defopt

@defopt pimacs-slash-commands

@lisp
'(("model" pimacs-select-model 0 "Switch models")
  ("new" pimacs-new-session 0 "Start a new session")
  ("reload" pimacs-reload 0 "Reload extensions, skills and prompts")
  ("resume" pimacs-resume 0 "Pick from previous sessions")
  ("compact" pimacs-compact 1 "Manually compact context, optionally with custom instructions")
  ("set-auto-compaction" pimacs-set-auto-compaction 0 "Set auto compaction")
  ("set-auto-retry" pimacs-set-auto-retry 0 "Set auto retry")
  ("session" pimacs-session-stats 0 "Show session file, ID, messages, tokens, and cost")
  ("name" pimacs-set-session-name 1 "Set session display name")
  ("set-thinking-level" pimacs-set-thinking-level 0 "Set thinking level")
  ("cycle-model" pimacs-cycle-model 0 "Cycle through available models")
  ("cycle-thinking-level" pimacs-cycle-thinking-level 0 "Cycle through thinking levels")
  ("set-steering-mode" pimacs-set-steering-mode 0 "Set steering mode")
  ("set-follow-up-mode" pimacs-set-follow-up-mode 0 "Set follow-up mode")
  ("fork" pimacs-fork 0 "Create a new session from a previous user message")
  ("clone" pimacs-clone 0 "Duplicate the current active branch into a new session")
  ("copy" pimacs-copy 0 "Copy last assistant message to clipboard")
  ("export" pimacs-export 1 "Export session to HTML")
  ("quit" pimacs-quit-chat 0 "Quit pimacs")
  ("exit" pimacs-quit-chat 0 "Quit pimacs"))
@end lisp

Alist mapping slash command names to command specs.

Each entry is (NAME COMMAND MAX-ARGS DESCRIPTION) where NAME is the command
string without the leading slash, COMMAND is a command symbol,
MAX-ARGS is 0 or 1 indicating the number of optional string
arguments the command accepts, and DESCRIPTION is a short
description string.
@end defopt

@defopt pimacs-insert-tool-args-functions

@lisp
'(("read" . pimacs--insert-read-args)
  ("write" . pimacs--insert-write-args)
  ("edit" . pimacs--insert-edit-args)
  ("bash" . pimacs--insert-bash-args)
  ("grep" . pimacs--insert-grep-args)
  ("find" . pimacs--insert-find-args)
  ("ls" . pimacs--insert-ls-args))
@end lisp

Alist mapping tool names to inserter functions.

Each entry is (TOOL-NAME . FUNCTION) where FUNCTION is called
with ARGS plist to insert formatted tool call arguments.
@end defopt

@defopt pimacs-insert-tool-result-functions

@lisp
'(("bash" . pimacs--insert-bash-result)
  ("read" . pimacs--insert-read-result)
  ("write" . pimacs--insert-write-result)
  ("edit" . pimacs--insert-edit-result)
  ("grep" . pimacs--insert-grep-result)
  ("find" . pimacs--insert-find-result)
  ("ls" . pimacs--insert-ls-result))
@end lisp

Alist mapping tool names to result inserter functions.

Each entry is (TOOL-NAME . FUNCTION) where FUNCTION is called
with (CONTENT DETAILS ARGS) to insert the tool execution result.
CONTENT is a list of content items.  Use @code{pimacs--insert-content} to render
it, or @code{pimacs--content-text} to extract text from content.
@end defopt

@defopt pimacs-visit-tool-result-functions

@lisp
'(("read" . pimacs--visit-read-result)
  ("write" . pimacs--visit-write-result)
  ("edit" . pimacs--visit-edit-result)
  ("grep" . pimacs--visit-grep-result))
@end lisp

Alist mapping tool names to result visitor functions.

Each entry is (TOOL-NAME . FUNCTION) where FUNCTION is called
with (DETAILS ARGS) to visit the relevant location of the tool result.
@end defopt

@defopt pimacs-visit-tool-call-functions

@lisp
'(("read" . pimacs--visit-read-call)
  ("write" . pimacs--visit-write-call)
  ("edit" . pimacs--visit-edit-call))
@end lisp

Alist mapping tool names to call visitor functions.

Each entry is (TOOL-NAME . FUNCTION) where FUNCTION is called
with (ARGS) to visit the relevant location of the tool call.
@end defopt

@defopt pimacs-insert-custom-message-functions @code{'()}

Alist mapping custom message types to inserter functions.

Each entry is (CUSTOM-TYPE . FUNCTION) where FUNCTION is called
with the message plist to insert the custom message content.
@end defopt

@defopt pimacs-send-pop-to-chat @code{t}

Whether to pop to the chat buffer after sending region, filename or errors.
@end defopt

@defopt pimacs-chat-keep-input-at-bottom @code{t}

Whether to keep the input area at the bottom as new chat content is added.
@end defopt

@defopt pimacs-list-sessions-table

@lisp
'(("Session" . (:session_name face font-lock-type-face))
  ("Provider" . :provider)
  ("Model" . :model)
  ("State" . (:agent_state face font-lock-constant-face))
  ("Context" . (:context_usage face shadow))
  ("Messages" . :total_messages)
  ("Cost" . (:cost face shadow))
  ("Project" . (:project_root face shadow)))
@end lisp

Columns displayed by @code{pimacs-list-sessions}.

Each entry is (HEADER . COMPONENT).  COMPONENT uses the same format as an
entry in @code{pimacs-header-line-format}.
@end defopt

@defopt pimacs-list-sessions-sort-key @code{'("Session" . nil)}

Initial sort order for @code{pimacs-list-sessions}.

The car is a header from @code{pimacs-list-sessions-table}.  A non-nil cdr sorts
in descending order.
@end defopt

@defopt pimacs-sync-request-timeout @code{2}

The number of seconds to wait for a sync response.
@end defopt

@defopt pimacs-executable @code{"pi"}

Pi command executable name.
@end defopt

@defopt pimacs-process-environment @code{'()}

List of extra environment variables to use when starting pimacs.
@end defopt

@defopt pimacs-flags @code{'()}

List of additional flags to provide when starting pimacs.
@end defopt

@defopt pimacs-log-rpc @code{nil}

When non-nil, log all RPC JSON to @code{pimacs-log-rpc-file}.
@end defopt

@defopt pimacs-log-rpc-file @code{(expand-file-name "pimacs.el.log" (temporary-file-directory))}

File to write RPC JSON log entries to.
@end defopt

@defopt pimacs-use-ansi-colors @code{t}

Whether to render ANSI colors in widget and status output.
@end defopt

@defopt pimacs-header-line-format @code{'(:context_usage " (" :compaction_mode ")" :spacer "(" :provider ") " :model " • " :thinking_level)}

Format of the Pimacs chat header line.

Strings are displayed literally.  Functions are called with the state
plist and their returned values are displayed.  A keyword component can
include text properties using @code{propertize} syntax.  For example:
"@code{(:model face font-lock-function-name-face)}".  A status component has
form @code{(:status STATUS-KEY PROPERTY VALUE...)}; it displays the text set
by an extension with STATUS-KEY and accepts optional text properties.
For example: @code{(:status "xyz-status" face font-lock-warning-face)}.

The following keywords are replaced with state information.

Session state keywords:
@table @code
@item :model
Model identifier.
@item :provider
Model provider.
@item :thinking_level
Thinking level.
@item :session_name
Session name.
@item :project_root
Project root directory.
@item :compaction_mode
@code{auto} or @code{manual}.
@item :message_count
Message count.
@item :pending_message_count
Pending message count.
@end table

Session statistics keywords:
@table @code
@item :user_messages
User message count.
@item :assistant_messages
Assistant message count.
@item :tool_calls
Tool call count.
@item :tool_results
Tool result count.
@item :total_messages
Total message count.
@item :input_tokens
Input token count.
@item :output_tokens
Output token count.
@item :cache_read_tokens
Cache-read token count.
@item :cache_write_tokens
Cache-write token count.
@item :cache_hit_percent
Total cache hit percentage.
@item :total_tokens
Total token count.
@item :cost
Session cost.
@item :context_usage
Context tokens and context window.
@item :context_tokens
Context token count.
@item :context_window
Context window size.
@end table

UI keywords:
@table @code
@item :agent_state
Current agent state.
@item :spinner
Active agent spinner.
@item :spacer
Space that right-aligns all following entries.
@end table

Status components:
@code{(:status STATUS-KEY ...)}  Extension status text for STATUS-KEY.

Use at most one @code{:spacer}.
@end defopt

@defopt pimacs-mode-line-format @code{'(" Pimacs " :agent_state :spinner)}

Format of the Pimacs mode-line entry.

See @code{pimacs-header-line-format} for available components.
@end defopt

@defopt pimacs-markdown-leading-newline-block-types

@lisp
'("pipe_table"
  "fenced_code_block"
  "indented_code_block"
  "block_quote"
  "list"
  "thematic_break"
  "html_block")
@end lisp

List of Markdown block node types.
Render these on a fresh line when the renderer starts mid-line.
@end defopt

@defopt pimacs-markdown-incremental-render-debug @code{nil}

Whether to capture incremental Markdown rendering diagnostics.

When non-nil, diagnostics are appended to the temporary buffer
@code{*pimacs-markdown-incremental-debug*}.
@end defopt

@defopt pimacs-markdown-use-unicode-tables @code{t}

Whether to render Markdown tables with Unicode borders.
@end defopt

@defopt pimacs-section-autohide-count @code{2}

Automatically hide older chat sections beyond this count.
This helps reduce clutter by collapsing earlier responses when the
conversation grows long.  When nil, auto hiding is disabled and no
sections are hidden automatically.
@end defopt

@defopt pimacs-section-autohide-filter @code{'all}

Filter controlling which sections are eligible for automatic hiding.

When set to @code{all}, every top-level section is eligible.

A value of @code{(:include TYPE...)} makes only the listed section types eligible.
A value of @code{(:exclude TYPE...)} makes every section type except the listed
types eligible.  A function value is called with each top-level section and
should return non-nil when that section is eligible.  Non-eligible sections
do not count toward @code{pimacs-section-autohide-count}.
@end defopt

@defopt pimacs-section-padding @code{"\n\n"}

String inserted between sections to control the visual gap.
Increase or decrease this value to adjust spacing between sections.
@end defopt

@defopt pimacs-section-visibility-indicators @code{'(pimacs-section-fringe-bitmap> . pimacs-section-fringe-bitmapv)}

Fringe bitmaps used to indicate section visibility.

The car is used for hidden sections and the cdr for visible sections.
Set this to nil to disable fringe indicators.
@end defopt

@defopt pimacs-section-type-faces

@lisp
'((root . pimacs-section-root-face)
  (thinking . pimacs-section-thinking-face)
  (thinking-level . pimacs-section-thinking-level-face)
  (assistant . pimacs-section-assistant-face)
  (user . pimacs-section-user-face)
  (tool-call . pimacs-section-tool-call-face)
  (tool-result . pimacs-section-tool-result-face)
  (error . pimacs-section-error-face)
  (model . pimacs-section-model-face)
  (compact . pimacs-section-compact-face)
  (info . pimacs-section-info-face)
  (custom . pimacs-section-custom-face)
  (queue . pimacs-section-queue-face)
  (notify . pimacs-section-notify-face)
  (select . pimacs-section-select-face)
  (confirm . pimacs-section-confirm-face)
  (input . pimacs-section-input-face)
  (session . pimacs-section-session-face))
@end lisp

Faces prepended to content in sections of each type.
@end defopt

@c custom-variables-end

@node Custom Faces
@chapter Custom Faces

@c custom-faces-start

@deffn Face pimacs-chat-role-face @code{'((t :inherit font-lock-builtin-face))}

Face used for generic chat role labels.
@end deffn

@deffn Face pimacs-chat-user-role-face @code{'((t :inherit font-lock-keyword-face))}

Face used for user chat message role labels.
@end deffn

@deffn Face pimacs-chat-assistant-role-face @code{'((t :inherit font-lock-constant-face))}

Face used for assistant chat message role labels.
@end deffn

@deffn Face pimacs-chat-title-face @code{'((t :inherit font-lock-builtin-face))}

Face used for titles.
@end deffn

@deffn Face pimacs-prompt-face @code{'((t :inherit widget-field))}

Face used for the Pimacs prompt input field.
@end deffn

@deffn Face pimacs-error-face @code{'((t :inherit error))}

Face used for Pimacs widget error messages.
@end deffn

@deffn Face pimacs-tool-name-face @code{'((t :inherit font-lock-function-name-face))}

Face used for tool names in tool execution events.
@end deffn

@deffn Face pimacs-grep-match-face @code{'((t :inherit match))}

Face used to highlight matching text in grep tool results.
@end deffn

@deffn Face pimacs-notify-info-face @code{'((t :inherit font-lock-comment-face))}

Face used for info notification messages.
@end deffn

@deffn Face pimacs-notify-warning-face @code{'((t :inherit warning))}

Face used for warning notification messages.
@end deffn

@deffn Face pimacs-notify-error-face @code{'((t :inherit error))}

Face used for error notification messages.
@end deffn

@deffn Face pimacs-widget-face @code{'((t :inherit shadow))}

Face used for extension widgets.
@end deffn

@deffn Face pimacs-status-face @code{'((t :inherit shadow))}

Face used for extension status.
@end deffn

@deffn Face pimacs-markdown-heading-face @code{'((t :inherit font-lock-function-name-face :weight bold))}

Face used for Markdown headings.
@end deffn

@deffn Face pimacs-markdown-inline-code-face @code{'((t :inherit (fixed-pitch font-lock-constant-face)))}

Face used for inline code.
@end deffn

@deffn Face pimacs-markdown-equation-face @code{'((t :inherit (fixed-pitch font-lock-constant-face)))}

Face used for Markdown equations.
@end deffn

@deffn Face pimacs-markdown-bold-face @code{'((t :inherit bold))}

Face used for bold text.
@end deffn

@deffn Face pimacs-markdown-italic-face @code{'((t :inherit italic))}

Face used for italic text.
@end deffn

@deffn Face pimacs-markdown-strike-through-face @code{'((t :strike-through t))}

Face used for Markdown strike-through text.
@end deffn

@deffn Face pimacs-markdown-superscript-face @code{'((t :height 0.8 :raise 0.3))}

Face used for Markdown superscript text.
@end deffn

@deffn Face pimacs-markdown-subscript-face @code{'((t :height 0.8 :raise -0.2))}

Face used for Markdown subscript text.
@end deffn

@deffn Face pimacs-markdown-link-face @code{'((t :inherit link))}

Face used for provisional Markdown links.
@end deffn

@deffn Face pimacs-markdown-list-marker-face @code{'((t :inherit shadow :slant normal :weight normal))}

Face used for Markdown list markers.
@end deffn

@deffn Face pimacs-markdown-checkbox-face @code{'((t :inherit font-lock-builtin-face))}

Face used for Markdown task-list checkboxes.
@end deffn

@deffn Face pimacs-markdown-blockquote-face @code{'((t :inherit font-lock-comment-face))}

Face used for Markdown blockquotes.
@end deffn

@deffn Face pimacs-markdown-horizontal-rule-face @code{'((t :inherit shadow))}

Face used for Markdown horizontal rules.
@end deffn

@deffn Face pimacs-markdown-code-block-face @code{'((t :inherit fixed-pitch))}

Face used as the base face of Markdown code blocks.
@end deffn

@deffn Face pimacs-markdown-table-header-face @code{'((t :inherit fixed-pitch))}

Face used for Markdown table headers.
@end deffn

@deffn Face pimacs-markdown-table-border-face @code{'((t :inherit fixed-pitch))}

Face used for Markdown table borders.
@end deffn

@deffn Face pimacs-section-root-face @code{'((t))}

Face applied to root sections.
@end deffn

@deffn Face pimacs-section-thinking-face @code{'((t :inherit shadow))}

Face applied to thinking sections.
@end deffn

@deffn Face pimacs-section-thinking-level-face @code{'((t :inherit shadow))}

Face applied to thinking-level sections.
@end deffn

@deffn Face pimacs-section-assistant-face @code{'((t))}

Face applied to assistant sections.
@end deffn

@deffn Face pimacs-section-user-face @code{'((t :inherit highlight :extend t))}

Face applied to user sections.
@end deffn

@deffn Face pimacs-section-tool-call-face @code{'((t))}

Face applied to tool call sections.
@end deffn

@deffn Face pimacs-section-tool-result-face @code{'((t))}

Face applied to tool result sections.
@end deffn

@deffn Face pimacs-section-error-face @code{'((t :inherit pimacs-error-face))}

Face applied to error sections.
@end deffn

@deffn Face pimacs-section-model-face @code{'((t :inherit shadow))}

Face applied to model sections.
@end deffn

@deffn Face pimacs-section-compact-face @code{'((t :inherit shadow))}

Face applied to compaction sections.
@end deffn

@deffn Face pimacs-section-info-face @code{'((t :inherit shadow))}

Face applied to information sections.
@end deffn

@deffn Face pimacs-section-custom-face @code{'((t))}

Face applied to custom sections.
@end deffn

@deffn Face pimacs-section-queue-face @code{'((t :inherit shadow))}

Face applied to queue sections.
@end deffn

@deffn Face pimacs-section-notify-face @code{'((t))}

Face applied to notification sections.
@end deffn

@deffn Face pimacs-section-select-face @code{'((t :inherit shadow))}

Face applied to selection sections.
@end deffn

@deffn Face pimacs-section-confirm-face @code{'((t :inherit shadow))}

Face applied to confirmation sections.
@end deffn

@deffn Face pimacs-section-input-face @code{'((t :inherit shadow))}

Face applied to input sections.
@end deffn

@deffn Face pimacs-section-session-face @code{'((t :inherit shadow))}

Face applied to session sections.
@end deffn

@c custom-faces-end

@bye
